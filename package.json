{
  "name": "ai-jail-sandbox",
  "version": "0.2.7",
  "description": "Sandbox segura para agentes de IA usando Docker",
  "main": "dist/cli.js",
  "bin": {
    "ai-jail-sandbox": "dist/cli.js"
  },
  "files": [
    "dist",
    "docker",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "vitest",
    "prepare": "npm run build"
  },
  "keywords": [
    "ai",
    "sandbox",
    "docker",
    "security",
    "gemini"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "execa": "^8.0.0"
  }
}
